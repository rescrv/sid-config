---
name: clippy
description:  Lint the codebase using the standard clippy command.
---
Run `cargo clippy --all-targets -- -D warnings -D clippy::all` and fix all issues, warnings, or errors it reports.
Run `cargo fmt` once all reports are addressed.
